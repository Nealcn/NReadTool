# AI 伴读记忆系统 — 全线架构方案

> 目标：一个**有记忆、能总结、会联想**的 AI 阅读伴侣

---

## 整体架构

```
能力递进叠加，数据模型一次设计到位：

书籍内容
  ├── 你划词 → AI 解释 → 自动存 annotation ──→ 关键词搜索 (Phase 1)
  │                                               ↓
  │                                          向量搜索 (Phase 2)
  │                                               ↓
  │                                          跨书联想 (Phase 2+)
  │
  ├── 你翻页 → 检测章节切换 → 触发总结 ────→ 章节摘要 (Phase 3)
  │                                               ↓
  │                                          全书摘要 (Phase 3)
  │
  └── 你合上书 → 后台聚合标注 + 摘要 ────→ 阅读报告 (Phase 4)
                                                   ↓
                                              主动联想推送 (Phase 4)

所有数据 → 同一个 SQLite（bookread.db）
所有 AI 调用 → 同一个 DeepSeek API
```

---

## 数据层

### `annotations` 表 — 你的阅读痕迹

```sql
CREATE TABLE annotations (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    book_id       INTEGER NOT NULL REFERENCES books(id),
    device_id     TEXT NOT NULL REFERENCES devices(device_id),
    chapter_idx   INTEGER,                    -- 章节索引
    cfi_range     TEXT,                       -- EPUB 定位（用于跳转回原文）
    selected_text TEXT NOT NULL,              -- 你选的词/句
    user_note     TEXT,                       -- 你写的笔记
    ai_response   TEXT,                       -- AI 当时的解释
    ai_type       TEXT DEFAULT 'sentence',    -- word/sentence/grammar/background
    embedding     TEXT,                       -- ★ Phase 2 填充：384维向量 JSON
    created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_annotations_book ON annotations(book_id);
CREATE INDEX idx_annotations_device ON annotations(device_id);
CREATE INDEX idx_annotations_book_device ON annotations(book_id, device_id);
```

### `book_summaries` 表 — AI 总结

```sql
CREATE TABLE book_summaries (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    book_id       INTEGER NOT NULL REFERENCES books(id),
    chapter_idx   INTEGER,                   -- NULL 表示全书总结
    chapter_title TEXT,
    summary_text  TEXT NOT NULL,
    model_used    TEXT DEFAULT 'deepseek-chat',
    token_count   INTEGER,
    created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(book_id, chapter_idx)
);
```

### `reading_sessions` 表 — 阅读轨迹

```sql
CREATE TABLE reading_sessions (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    book_id       INTEGER NOT NULL REFERENCES books(id),
    device_id     TEXT NOT NULL REFERENCES devices(device_id),
    start_time    DATETIME NOT NULL,
    end_time      DATETIME,
    chapters_read TEXT,                      -- JSON array
    page_progress REAL,                      -- 0.0 ~ 1.0
    UNIQUE(book_id, device_id, start_time)
);
```

---

## API 层

### Phase 1 — 标注 + 关键词搜索

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/v1/annotations` | 创建标注 |
| GET | `/api/v1/annotations?book_id=X&q=keyword` | 关键词搜索 |
| GET | `/api/v1/annotations/:id` | 单条详情 |
| PUT | `/api/v1/annotations/:id` | 更新笔记 |
| DELETE | `/api/v1/annotations/:id` | 删除 |

### Phase 2 — 语义搜索（追加）

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/v1/annotations/search?q=...&semantic=true` | 向量语义搜索 |
| GET | `/api/v1/annotations/related/:id?cross_book=true` | 跨书相似标注 |

### Phase 3 — 自动总结（追加）

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/v1/sessions` | 开始阅读会话 |
| PUT | `/api/v1/sessions/:id` | 结束会话 |
| POST | `/api/v1/books/:id/summarize/chapter/:idx` | 触发章节总结 |
| GET | `/api/v1/books/:id/summaries` | 获取总结列表 |

### Phase 4 — 主动推送（追加）

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/v1/annotations/insights?since=...` | 阅读洞察/周报 |
| GET | `/api/v1/cross-links?device_id=X` | 跨书联想列表 |

---

## 核心技术选型

### 搜索策略

```
Phase 1（现在就做）:
  SELECT * FROM annotations
  WHERE book_id = ? AND selected_text LIKE '%' || ? || '%'
  → 中文 LIKE 子串匹配，比 FTS5 逐字拆分准得多
  → 几千条数据全表扫描 < 10ms，不需要索引

Phase 2（以后叠加）:
  SELECT * FROM annotations
  WHERE embedding IS NOT NULL
  ORDER BY cosine_similarity(embedding, ?) DESC LIMIT 20
  → 向量语义搜索，关键词搜不到的概念也能找到
```

### Embedding 方案

- **库**: `fastembed`（~5MB，无 PyTorch，已安装 ✅）
- **模型**: `BAAI/bge-small-zh-v1.5`（384维，中文优化，首次自动下载 ~33MB）
- **存储**: JSON TEXT 存 SQLite，Python 算余弦相似度，< 10ms/千条
- **为什么不用 sqlite-vec**: 避免 Windows 预编译 .dll 的依赖风险

### 跨设备同步

短期：导出/导入按钮，手动迁移
长期：轻量云同步（基于 device_id merge，不需要账号体系）

### 自动总结触发

```
检测到章节切换（spine_index 变化）：
  → 取本章 plain_text（前 3000 字）
  → + 本章所有 annotations
  → → DeepSeek 总结
  → → 存 book_summaries
  → → 前端通知："第三章摘要已生成"
```

---

## 实施路线图

| 阶段 | 内容 | 预估工时 |
|------|------|---------|
| **Phase 1** | annotations 表 + CRUD API + LIKE 搜索 | 后端 0.5天 |
| | 前端：AI 自动保存 + 搜索入口 + 笔记编辑 | 前端 2天 |
| **Phase 2** | fastembed 集成 + 向量搜索 + 跨书联想 | 后端 1天 |
| | 前端：语义搜索 UI + 关联展示 | 前端 1天 |
| **Phase 3** | reading_sessions + 自动总结 Pipeline | 后端 1天 |
| | 前端：摘要展示 + 阅读统计 | 前端 1天 |
| **Phase 4** | 主动联想 + 阅读报告 + 通知系统 | 后端+前端 2天 |

**全部做完约 8.5 天工作日**

---

## 关键文件清单

### 后端

| 文件 | 说明 |
|------|------|
| `backend/app/models/annotation.py` | Annotation ORM |
| `backend/app/models/book_summary.py` | BookSummary ORM |
| `backend/app/models/reading_session.py` | ReadingSession ORM |
| `backend/app/models/__init__.py` | 导入新模型 |
| `backend/app/schemas/annotation.py` | Pydantic 模型 |
| `backend/app/schemas/summary.py` | Pydantic 模型 |
| `backend/app/api/v1/annotations.py` | 标注 CRUD + 搜索 |
| `backend/app/api/v1/sessions.py` | 会话 API |
| `backend/app/api/v1/summaries.py` | 总结 API |
| `backend/app/api/v1/router.py` | 注册路由 |
| `backend/app/services/annotation_service.py` | 标注业务逻辑 |
| `backend/app/services/summary_service.py` | 总结 Pipeline |
| `backend/app/services/embedding_service.py` | fastembed 封装 |

### 前端

| 文件 | 说明 |
|------|------|
| `services/api/annotations.ts` | 标注 API 封装 |
| `components/annotator/Annotator.tsx` | AI 结果后自动保存 |
| `components/sidebar/SearchView.tsx` | 搜索面板 |
| `components/sidebar/SummaryView.tsx` | 摘要展示 |
| `hooks/useAnnotationSearch.ts` | 搜索逻辑 |

---

## 验证方法

1. **Phase 1**：划词 → AI 解释 → 自动存库 → 搜索返回 → 点击跳转原文
2. **Phase 2**：搜"宇宙法则"匹配"黑暗森林"；跨书概念关联
3. **Phase 3**：读完一章自动生成摘要，书架可见
4. **Phase 4**：新增标注时提示跨书关联；周报展示阅读统计
